flask>=3.0
duckdb>=1.0
bcrypt>=4.0
python-dotenv>=1.0
flask-limiter>=3.0
flasgger>=0.9
pandas>=2.0
openpyxl>=3.0
APScheduler>=3.10
sentry-sdk>=2.0
reportlab>=4.0
gunicorn>=22.0
# ── Phase 0-1 migration tooling (DuckDB → Postgres) ──
alembic>=1.13
SQLAlchemy>=2.0
psycopg[binary]>=3.1
# ── Phase 3a auth hardening (SRS CC-06) ──
argon2-cffi>=23.1
redis>=5.0
